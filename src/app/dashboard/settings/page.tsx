import { PageShell, HubHero } from '@/components/Ui';

export default function DashboardSettingsPlaceholder() {
  return (
    <PageShell>
      <main className="dashboard-page">
        <HubHero title="Settings" copy="This page is ready to be built." />
      </main>
    </PageShell>
  );
}
